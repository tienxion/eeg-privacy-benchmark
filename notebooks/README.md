# Notebooks

Use notebooks only for short-lived exploration.

Rules:
- keep one question per notebook
- move reusable code into `src/`
- do not treat notebooks as the source of truth for final experiments
